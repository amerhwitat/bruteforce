# Apple application

Native SwiftUI/Xcode research shell for the safe cryptographic test harness. It preserves the repository boundary against private-key recovery, seed guessing, or unauthorized credential attacks.

Targets: iOS/iPadOS and macOS. IPA export requires macOS/Xcode.
