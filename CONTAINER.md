# Containerized version

```bash
docker compose build
docker compose up
```

The container is a non-root Python runtime and does not add new attack capabilities. Use the repository's existing authorization and safety controls. Native operation remains unchanged.
