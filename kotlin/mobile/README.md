# Crypto Research Kotlin Mobile

Android Kotlin mobile harness for deterministic, synthetic cryptographic research. This implementation preserves the repository safety boundary: no private-key cracking, seed guessing, unauthorized wallet recovery, or credential attacks.

It is isolated under `kotlin/mobile/` and can later share common conformance logic with the other language implementations through Kotlin Multiplatform.
